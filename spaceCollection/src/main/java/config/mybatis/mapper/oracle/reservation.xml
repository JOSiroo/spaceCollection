<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sc.spaceCollection.reservation.model.ReservationDAO">
	<insert id="insertReservation" parameterType="reservationVo">
		<selectKey keyProperty="reservationNum" order="BEFORE" resultType="int">
			select reservation_seq.nextval from dual
		</selectKey>
		insert into reservation(RESERVATION_NUM,USER_NUM,SD_NUM,RESERVE_START_DAY,RESERVE_START_HOUR,
								RESERVE_FINISH_DAY,RESERVE_FINISH_HOUR,RESERVE_PRICE,RESERVER_PAY_DAY,PAYMENT_NUM)
		values(#{reservationNum}, #{userNum}, #{sdNum}, #{reserveStartDay},#{reserveStartHour},
		#{reserveFinishDay},#{reserveFinishHour},#{reservePrice},sysdate,#{paymentNum})
	</insert>
</mapper>
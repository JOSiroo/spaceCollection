<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sc.spaceCollection.collection.model.CollectionDAO">
	<select id="selectCollectionName" parameterType="int" resultType="string">
		select sc_type from exhibition
		where sc_num = #{scNum}
	</select>
	<select id="showCollectionList" resultType="map">
		select * 
		from(
			select A.*, rownum as rn
			from 
			(
				select s.*, e.sc_num 
				from exhibition e,
					SPACE_SD_AVERAGEPRICE_FACILITY_VIEW s
				where sc_num = #{scNum}  
			)A	
		)
		<![CDATA[
		where rn >= #{startRow} and rn <= #{endRow} ]]>
	</select>
	
</mapper>